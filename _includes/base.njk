<!DOCTYPE html>
<html lang='en'>
    <head>
        <!-- Title -->
        <title>Camber's {{ title | safe }}</title>
        <link
        rel="icon" type="image/png" href="{{ '/static/imgs/pfp-ico.png' | url }}"> <!-- Display information -->
        <meta charset="UTF-8">
        <meta
        name="viewport" content="width=device-width, initial-scale=1"> <!-- Stylesheets -->
        <link rel="stylesheet" href="{{ '/static/styles/default.css' | url }}"> {% block additional_styles %}{% endblock %}
        <!-- Icons -->
        <script src="https://kit.fontawesome.com/c938d70fe8.js" crossorigin="anonymous"></script>
    </head>
    <body>
        <header>
            <h1>Camber's Creations</h1>
            <nav>
                <a href="{{ '/' | url }}">Home</a>
                <a href="{{ '/gallery/' | url }}">Illustration</a>
                <a href="{{ '/web/' | url }}">Websites</a>
                <a href="{{ '/game-development/' | url }}">Games</a>
            </nav>
        </header>
        <main>
            {{ content | safe }}
        </main>
    </body>
</html>